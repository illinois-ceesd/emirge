name: CI test

# Trigger the workflow on push and daily cron job
on:
  push:
  schedule:
  - cron:  '5 0 * * *'

jobs:
  build:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
    - uses: actions/checkout@v2
    - name: Install prerequisites
      run: ./install.sh
    - name: Version info
      run: |
        source $HOME/miniforge3/bin/activate dgfem
        ./version.sh
    - name: Run wave-eager
      run: |
        # export PATH=$HOME/miniforge3/bin:$PATH
        source $HOME/miniforge3/bin/activate dgfem
        export LD_LIBRARY_PATH=$HOME/miniforge3/envs/dgfem/lib
        which python
        export OCL_ICD_VENDORS=$HOME/miniforge3/envs/dgfem/etc/OpenCL/vendors/
        git clone https://github.com/illinois-ceesd/mirgecom
        cd mirgecom
        pip install -e .
        python examples/wave-eager.py
